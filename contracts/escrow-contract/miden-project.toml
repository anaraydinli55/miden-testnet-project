[package]
name = "escrow-contract"
version = "0.1.0"

[lib]
kind = "account-component"
namespace = "miden:escrow-contract/escrow-contract@0.1.0"

[dependencies]
miden-core = "*"
miden-protocol = "*"

[package.metadata.miden]
supported-types = ["RegularAccountUpdatableCode"]